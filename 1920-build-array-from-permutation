class Solution {
    public int[] buildArray(int[] nums) {
        int j=0;
        int res[] = new int[nums.length];
        for(int i=0;i<nums.length;i++){
            res[j++] = nums[nums[i]];
        }
        return res;
    }
}
